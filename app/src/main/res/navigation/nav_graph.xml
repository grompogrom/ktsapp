<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/welcomeScreen">
    <fragment
        android:id="@+id/welcomeScreen"
        android:name="com.pogrom.ktsapp.fragments.WelcomeScreenFragment"
        android:label="fragment_welcome_screen"
        tools:layout="@layout/fragment_welcome_screen" >
        <action
            android:id="@+id/action_welcomeScreen_to_authFragment"
            app:destination="@id/authFragment"
            app:popUpTo="@id/welcomeScreen"
            app:popUpToInclusive="true" />
    </fragment>
    <fragment
        android:id="@+id/MainFragment"
        android:name="com.pogrom.ktsapp.fragments.MainFragment"
        android:label="Paging3Fragment" />
    <fragment
        android:id="@+id/authFragment"
        android:name="com.pogrom.ktsapp.auth.AuthFragment"
        android:label="AuthFragment" >
        <action
            android:id="@+id/action_authFragment_to_MainFragment"
            app:destination="@id/MainFragment"
            app:popUpTo="@id/authFragment"
            app:popUpToInclusive="true" />
    </fragment>
</navigation>